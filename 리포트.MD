# AI 최종판단 섹션 UX 개선 — Implementation Plan v3

> Step 4 국가상세단계의 **UI 패턴 + 데이터**를 모두 참고하여 재설계

---

## 핵심 발견: 데이터 갭

| 데이터 | Step 4 사용 | Step 6 현재 |
|---|---|---|
| `country_decision_facts` (시장·관세·물류·인증·규제) | ✅ `DecisionFact[]`로 로드 → UI 시각화 | ⚠️ 로드하지만 **AI 입력용으로만 전달**, UI에 안 보여줌 |
| `country_verdicts` (국가별 AI 최종판단) | ✅ `AiFinalVerdict` 카드로 렌더링 | ❌ **아예 로드하지 않음** |
| `buildMarketEvidence()` (시장 규모·한국 점유율) | ✅ 차트로 시각화 | ❌ 사용 안 함 |
| `buildTariffRangeEvidence()` (관세율 범위) | ✅ HTS 후보 카드로 시각화 | ❌ 사용 안 함 |
| `buildLogisticsEvidence()` (LPI 물류 점수) | ✅ 레이더 차트로 시각화 | ❌ 사용 안 함 |
| `buildUsitcHtsEvidence()` (미국 HTS 10자리 후보) | ✅ 세번 목록으로 시각화 | ❌ 사용 안 함 |

> [!CAUTION]
> **문제:** Step 4에서 이미 수집·분석한 풍부한 데이터(시장 $10.5B, 한국 점유율 5.57%, 관세 Free, LPI 3.8 등)가 리포트에서는 AI 내부 입력으로만 쓰이고, 사용자에게 직접 보이지 않습니다.

---

## 개선 전략 4가지

### 전략 A: Hero Metrics Panel — 수출 판단 요약 3열 카드

Step 4의 `MetricCard` 3열 패턴을 리포트에 적용합니다.

```
┌─────────────────────────────────────────────────────────┐
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐    │
│  │ 수출 판단     │ │ 신뢰도       │ │ 근거 현황     │    │
│  │ 🟡 조건부 추진│ │ 보통         │ │ P 6건 · W 5건│    │
│  │ 일부 조건 확인│ │ 이슈 2건으로 │ │ 미확인 3건    │    │
│  │ 후 추진 가능  │ │ 1단계 하향   │ │              │    │
│  └──────────────┘ └──────────────┘ └──────────────┘    │
│                                                         │
│  미국 타이어 시장 진출은 유망하나, 반덤핑 관세 및       │
│  DOT 인증 요건에 대한 철저한 사전 대비가 필요합니다.    │
└─────────────────────────────────────────────────────────┘
```

---

### 전략 B: Step 4식 AI 판단 카드 — 핵심 근거/위험/실행 방향/공식자료

기존 `decisionReasons[]` + `immediateActions[]`를 Step 4의 `AiFinalVerdictCard` 패턴으로 재구성합니다.

```
┌─ 핵심 판단 근거 ──────────┐  ┌─ 주요 위험 요소 ──────────┐
│ 🟢 한국산 점유율 5.57%    │  │ 🟡 반덤핑 관세 부과 중     │
│   [UN Comtrade]            │  │   → 관세사 확인 후 가격 전  │
│ 🟢 한미FTA 관세 Free      │  │     략 수립 필요            │
│   [USITC HTS]              │  │ 🟡 DOT 인증 미보유         │
│ 🟢 LPI 3.8/5 물류 양호    │  │   → KOTRA 인증지원사업 활용│
│   [World Bank]             │  │   [KOTRA]                  │
└────────────────────────────┘  └────────────────────────────┘

┌─ 권장 실행 방향 ──────────────────────────────────────────┐
│ 1. HTS 10자리 세번 확정 (우선)                           │
│    타이어 구조와 림 직경으로 미국 관세사와 확정 필요      │
│ 2. DOT 인증 및 FMVSS 준수 확인 (우선)                    │
│    통관 거부 및 판매 불가 방지 위한 필수 요건             │
│ 3. 반덤핑 관세 일몰 재심 모니터링                        │
│    관세율 변동 시 수익성 직접 영향                        │
└──────────────────────────────────────────────────────────┘

┌─ 참고 공식자료 ───────────────────────────────────────────┐
│ 🔗 미국 상무부  🔗 USITC  🔗 DOT  🔗 NHTSA  🔗 KOTRA    │
└──────────────────────────────────────────────────────────┘
```

---

### 전략 C: Step 4 데이터 통합 표시 (**신규 — 핵심 변경**)

**`country_verdicts` + `country_decision_facts`에서 이미 수집된 데이터를 리포트 UI에 직접 시각화**합니다.

#### C-1. `country_verdicts` 데이터 로드 (DB 추가 조회)

```typescript
// Step6Report.tsx loadReportBundle 함수에 추가
const { data: countryVerdictRows } = await supabase
  .from("country_verdicts")
  .select("country_code, verdict, evidence_hash, created_at")
  .eq("project_id", id);
```

→ `Bundle` 타입에 `countryVerdicts` 필드 추가

#### C-2. Step 4 인사이트 빌더 재활용

이미 `country_decision_facts`를 로드하고 있으므로, 기존 인사이트 빌더 함수를 **UI 표시용으로 호출**만 하면 됩니다:

```typescript
// 현재: AI 입력용으로만 전달
// 개선: UI에서도 사용
import {
  buildMarketEvidence,
  buildTariffRangeEvidence,
  buildLogisticsEvidence,
  buildUsitcHtsEvidence,
  groupDecisionFactsForService,
} from "@/lib/country-decision-insights";

// 국가별 DecisionFact에서 인사이트 추출
const factsForCountry = bundle.decisionFacts.filter(f => f.countryCode === code);
const marketEvidence = buildMarketEvidence(factsForCountry);    // 시장 규모·한국 점유율
const tariffEvidence = buildTariffRangeEvidence(factsForCountry); // 관세율 범위
const logisticsEvidence = buildLogisticsEvidence(factsForCountry); // LPI 물류 점수
const usitcEvidence = buildUsitcHtsEvidence(factsForCountry);   // HTS 후보
```

#### C-3. 리포트에 표시할 Step 4 데이터 요약 카드

```
┌─ 국가 상세 데이터 요약 (US) ─────────────────────────────┐
│                                                          │
│  시장 기회              관세 현황          물류 환경      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │ 전체 수입     │  │ 일반세율 4%  │  │ LPI 3.8/5    │   │
│  │ $10.51B       │  │ 한국 특혜    │  │ 통관 3.7     │   │
│  │ 한국산 5.57%  │  │ Free (FTA)   │  │ 인프라 3.9   │   │
│  │ $585.4M       │  │ 추가관세 25% │  │ 국제운송 3.4 │   │
│  └──────────────┘  └──────────────┘  └──────────────┘   │
│                                                          │
│  결제 위험                                               │
│  ┌────────────────────────────────────────────────┐     │
│  │ 국가신용등급 1 · 지연율 18.9% · 평균 70.4일    │     │
│  │ 주요 결제조건: O/A(T/T 포함) 85.7%             │     │
│  └────────────────────────────────────────────────┘     │
│                                                          │
│  국가별 AI 판단 (Step 4)                                 │
│  ┌────────────────────────────────────────────────┐     │
│  │ 🟡 조건부 진출 가능 · 신뢰도 높음              │     │
│  │ 미국 시장은 높은 수요와 한국산 제품에 대한...   │     │
│  │ 생성 2026.07.24 · Gemini                       │     │
│  └────────────────────────────────────────────────┘     │
│                                                          │
│  📎 국가 상세분석 전체 보기 →                            │
└──────────────────────────────────────────────────────────┘
```

> [!TIP]
> 이 카드는 **데이터를 새로 수집하는 것이 아닙니다.** Step 4에서 이미 수집해둔 `country_decision_facts`와 `country_verdicts` DB 데이터를 읽어서 요약 표시하는 것입니다.

---

### 전략 D: Gate Dashboard — 6대 게이트 대시보드

v2와 동일. `draft.decisionGates[]`를 3×2 시각 그리드로 표시합니다.

---

## Proposed Changes

### Phase 1: 데이터 레이어 (Step 4 데이터 로드)

#### [MODIFY] [Step6Report.tsx](file:///c:/Users/dbcdk/Desktop/%EC%A0%9C14%ED%9A%8C%20%EC%82%B0%EC%97%85%ED%86%B5%EC%83%81%EB%B6%80%20%EA%B3%B5%EA%B3%B5%EB%8D%B0%EC%9D%B4%ED%84%B0%20%EC%95%84%EC%9D%B4%EB%94%94%EC%96%B4%20%EA%B3%B5%EB%AA%A8%EC%A0%84/src/pages/Step6Report.tsx)

**`loadReportBundle` 함수 (라인 228~264):**

1. `country_verdicts` 테이블 추가 조회
   ```typescript
   supabase
     .from("country_verdicts")
     .select("country_code, verdict, evidence_hash, created_at")
     .eq("project_id", id)
   ```

2. `Bundle` 인터페이스에 `countryVerdicts` 필드 추가
   ```typescript
   countryVerdicts: Array<{
     countryCode: string;
     verdict: AiFinalVerdict;
     createdAt: string;
   }>;
   ```

3. `parseVerdictResponse` 유틸 재사용 (이미 `country-decision-verdict.ts`에 존재)

---

### Phase 2: 인사이트 빌드 레이어

#### [MODIFY] [Step6Report.tsx](file:///c:/Users/dbcdk/Desktop/%EC%A0%9C14%ED%9A%8C%20%EC%82%B0%EC%97%85%ED%86%B5%EC%83%81%EB%B6%80%20%EA%B3%B5%EA%B3%B5%EB%8D%B0%EC%9D%B4%ED%84%B0%20%EC%95%84%EC%9D%B4%EB%94%94%EC%96%B4%20%EA%B3%B5%EB%AA%A8%EC%A0%84/src/pages/Step6Report.tsx)

**`DecisionReportContent` 컴포넌트 내부 (라인 719 부근):**

```typescript
import {
  buildMarketEvidence,
  buildTariffRangeEvidence,
  buildLogisticsEvidence,
  buildUsitcHtsEvidence,
} from "@/lib/country-decision-insights";

// 이미 로드된 decisionFacts에서 인사이트 추출
const countryInsights = useMemo(() => {
  return bundle.countries.map(country => {
    const facts = bundle.decisionFacts
      .filter(f => f.countryCode === country.country_code);
    return {
      countryCode: country.country_code,
      countryName: country.country_name,
      market: buildMarketEvidence(facts),
      tariff: buildTariffRangeEvidence(facts),
      logistics: buildLogisticsEvidence(facts),
      usitcHts: buildUsitcHtsEvidence(facts),
      verdict: bundle.countryVerdicts
        .find(v => v.countryCode === country.country_code) ?? null,
    };
  });
}, [bundle]);
```

---

### Phase 3: UI 컴포넌트 (전략 A + B + C + D)

#### [MODIFY] [Step6Report.tsx](file:///c:/Users/dbcdk/Desktop/%EC%A0%9C14%ED%9A%8C%20%EC%82%B0%EC%97%85%ED%86%B5%EC%83%81%EB%B6%80%20%EA%B3%B5%EA%B3%B5%EB%8D%B0%EC%9D%B4%ED%84%B0%20%EC%95%84%EC%9D%B4%EB%94%EC%96%B4%20%EA%B3%B5%EB%AA%A8%EC%A0%84/src/pages/Step6Report.tsx)

**"2. AI 최종판단" 영역 (라인 771~812) 전면 재구성:**

1. **`ReportHeroMetrics`** — 전략 A
   - 수출 판단 / 신뢰도 / 근거 현황 3열 MetricCard
   - Step 4의 `MetricCard` 패턴 동일 적용

2. **`ReportBasisSection`** — 전략 B
   - `AiFinalVerdictCard.BasisSection` 패턴 복제
   - `decisionReasons.filter(type === "opportunity")` 매핑

3. **`ReportRiskSection`** — 전략 B  
   - `AiFinalVerdictCard.RiskSection` 패턴 복제
   - 각 위험에 `→ 대응방안` 서브라인

4. **`ReportActionSection`** — 전략 B
   - `AiFinalVerdictCard.ActionSection` 패턴 복제
   - 번호매김 + `우선` 배지

5. **`ReportOfficialSources`** — 전략 B
   - 기관명 클릭 링크 배지 (`P-*`/`W-*` 코드 대체)

6. **`ReportCountryInsightsSummary`** — 전략 C (**신규**)
   - 각 국가별: 시장 기회(수입액·한국 점유율) + 관세(일반·특혜·추가) + 물류(LPI) + 결제위험 요약 카드
   - Step 4 AI 판단(`country_verdicts`) 요약 배너
   - "국가 상세분석 전체 보기 →" 링크

7. **`GateDashboard`** — 전략 D
   - 6대 게이트 3×2 시각 그리드

---

### 섹션 번호 재조정

| # | Before | After |
|---|---|---|
| 1 | 기본 정보 | 기본 정보 (유지) |
| 2 | AI 최종판단 | **AI 최종판단** (Hero Metrics + 핵심근거/위험 2열 + 권장실행 + 공식자료) |
| 3 | AI 판단 이유 | **국가 상세 데이터 요약** (시장·관세·물류·결제·Step4 AI판단) |
| 4 | — | **의사결정 게이트** (6대 게이트 대시보드) |
| 5 | AI 추천 진입전략 | AI 추천 진입전략 (유지) |
| 6 | 근거 보기 | 근거 보기 (유지) |

---

## Open Questions

> [!IMPORTANT]
> **Q1.** Step 4의 `country_verdicts`가 없는 경우(사용자가 Step 4에서 AI 판단을 생성하지 않았을 때) 어떻게 처리할까요?
> - A) 해당 영역을 "국가 상세분석에서 AI 판단을 먼저 생성하세요" 안내 문구로 표시
> - B) 영역 자체를 숨기고, 시장/관세/물류 데이터만 표시 (DecisionFacts는 있을 수 있으므로)

> [!IMPORTANT]
> **Q2.** 국가 상세 데이터 요약(전략 C)을 별도 섹션 "3. 국가 상세 데이터 요약"으로 분리할지, "2. AI 최종판단" 안에 넣을지?
> - 분리: 각 섹션이 명확하게 구분되어 길이 관리 용이
> - 통합: 판단 근거와 데이터가 한 곳에 모여 설득력 높음

---

## Verification Plan

### Automated Tests
```bash
npx vitest run src/test/report-decision-v2.test.ts
```

### Manual Verification
- [ ] `country_verdicts` 데이터가 있는 국가의 Step 4 AI 판단 요약 표시 확인
- [ ] `country_verdicts` 데이터가 없는 국가의 fallback 표시 확인
- [ ] 시장 데이터 카드에 수입액·한국 점유율 정확히 표시되는지 확인
- [ ] 관세 카드에 일반세율·FTA 특혜·추가관세 표시 확인
- [ ] LPI 물류 점수 카드 정상 표시 확인
- [ ] 모바일(375px)에서 모든 카드 반응형 확인
- [ ] PDF 다운로드 시 모든 데이터 카드 포함 확인
- [ ] 공식자료 링크가 새 탭으로 열리는지 확인
